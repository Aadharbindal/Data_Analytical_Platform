import logging

logger = logging.getLogger("ResamplingEngine")

class ResamplingEngine:
    """Handles frequency conversion (e.g. daily -> monthly)."""
    pass

resampling_engine = ResamplingEngine()
